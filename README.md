Very simple TCP client server "hello world" application over sockets in python.

```
# Following would start a server in background.
$ python3 server.py & 

# Once server is started run client as follows:
$ python3 client.py
```